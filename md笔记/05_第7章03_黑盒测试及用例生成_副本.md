# 第7章03 黑盒测试及用例生成

## 5.1 黑盒测试概述

黑盒测试是一种==基于规格说明==（Spec）、不要求考察代码、以用户视角进行的测试。又称**功能测试、基于规格说明的测试**。

黑盒测试依据：需求规格说明；用户视角；输入与输出关系；明确需求和隐含需求。

能发现其他测试遗漏的**逻辑缺陷**，如因果关系问题；

适用于单元、集成、系统、回归等多个阶段。

黑盒测试意义：黑盒测试有助于软件产品**总体功能验证**：

1. 检查**明确需求和隐含需求**；
2. 采用**有效输入和无效输入**；
3. 包含用户视角。

黑盒测试步骤：

1. 熟悉规格说明书，理解测试需求；
2. 生成测试用例；
3. 执行测试；
4. 判定测试结果。

#### 黑盒测试有什么特点

黑盒测试特点：**一种基于需求的测试**

目的是确认软件需求规格说明书列出的需求是否都正确实现

优点：==与软件具体实现无关==，代码变化时测试用例仍可能复用；测试用例设计可与软件实现并行；

#### 如何实施黑盒测试

黑盒测试实施分为三类：

1. 正面测试；
2. 负面测试；
3. 边界值测试。

| 对比项                 | 作用                                                         | 目的                         |
| ---------------------- | ------------------------------------------------------------ | ---------------------------- |
| 正面测试和正面测试用例 | 验证系统==在正常有效的输入条件下==能否按预期执行功能         | 证明系统能做它应该做的事     |
| 负面测试和负面测试用例 | 验证系统==在异常无效的输入条件下==能否优雅地处理错误而不崩溃 | 确保系统不会做它不应该做的事 |

| **对比项** | **正面测试**             | **负面测试**                     |
| ---------- | ------------------------ | -------------------------------- |
| 测试条件   | 根据需求规格说明         | 条件在需求规格说明之外           |
| 目的       | 证明系统能做它应该做的事 | 确保系统不会做它不应该做的事     |
| 覆盖率计算 | **覆盖需求和条件**       | ==通常没有覆盖率==               |
| 挑战性     | 根据规格说明设计用例     | 需要创造性产生尽可能多的非法输入 |

## 5.2 黑盒测试与白盒测试区别

| 维度 | 黑盒测试 | 白盒测试 |
| ---- | -------- | -------- |
| 依据 | 需求规格说明 | 源程序或代码结构 |
| 是否关注内部实现 | 不关注 | 关注 |
| 关注点 | 输入、输出、功能行为 | 语句、分支、条件、路径、数据流 |
| 典型方法 | 等价类、因果图、判定表、边界值 | 控制流覆盖、基本路径、数据流测试 |

黑盒测试可以检测需求文档不完整导致的功能缺失问题；白盒测试在系统测试阶段也常需要与黑盒测试结合。

灰盒测试：部分可见内部结构。介于两者之间，测试者**了解模块间的交互、数据库设计或算法概要，但无需深入每一行代码**。常见于==集成测试或接口测试==，比如检查用户注册后，数据是否按预期写入了数据库的某个表

## 5.3 黑盒测试用例设计和生成方法

### 5.3.1 等价类划分

#### 等价类定义

等价类是输入域的某个子集，该子集中的每个输入数据对揭露软件错误都是等效的。测试等价类中的一个代表值，通常可代表该类其他值。

等价类：测试相同目标或暴露相同软件缺陷的一组测试

另一种表达：如果软件行为对一组值相同，则这组值称为等价类；产生相同预期输出的一组输入值叫一个等价类划分。

#### 如何进行等价类划分

将程序的==输入域==**划分为等价类**，以便导出测试用例

它试图通过**设计一个测试用例来尽可能发现多个错误**，从而减少测试用例数目，降低测试工作量。

#### 有效等价类和无效等价类

| 类型 | 含义 | 作用 |
| ---- | ---- | ---- |
| 有效等价类 | 完全满足规格说明的输入数据集合 | ==检验程序是否满足规格说明== |
| 无效等价类 | 不满足程序输入要求的输入数据集合 | ==检验程序能否处理非法输入== |

#### 等价类划分准则

客观题重点。

| 输入条件 | 有效等价类 | 无效等价类 |
| -------- | ---------- | ---------- |
| 布尔表达式 | 1 个有效等价类 | 1 个无效等价类 |
| 输入代表一个范围 | 1 个有效等价类 | 2 个无效等价类：小于范围、大于范围 |
| 输入数据个数有规定 | 1 个有效等价类 | 2 个无效等价类：少于、多于 |
| 输入代表集合的某个子集 | 1 个有效等价类 | 1 个或多个无效等价类 |
| 输入条件是一组列表数据 | N 个有效等价类 | 1 个无效等价类 |
| 输入条件要求符合若干规则 | 多个有效等价类 | 若干无效等价类 |

#### 根据等价类生成测试用例

课件步骤：

1. 选择**划分准则**；
2. 根据准则确定**有效等价类和无效等价类**；
3. 从等价类中选取**样本数据**覆盖所有等价类；
4. **根据需求写预期结果**；
5. 执行测试。

重点规则：

- 每个有效等价类都要覆盖；
- 每个无效等价类都要==单独覆盖==。

#### 等价类划分主观题答题模板

1. 写出被测功能的输入条件和输出要求。
2. 按范围、集合、个数、规则等分类划分有效等价类和无效等价类。
3. 画等价类表：编号、输入条件、等价类类型、代表数据、预期结果。
4. 设计测试用例：有效等价类可尽量合并覆盖；无效等价类单独覆盖。
5. 给出预期输出和错误处理结果。
6. 检查是否遗漏边界值，必要时说明边界值分析作为补充。

常见扣分点：

- 只列有效类，不列无效类；
- 多个无效等价类放在一个测试用例里，导致不能定位错误来源；
- 忘记写预期结果；
- 没有根据题目规格划分，而是凭经验泛泛分类。

eg对应等价类：

| **序号** | **等价类** | **输入类型** | **测试数据** | **预期结果**    |
| -------- | ---------- | ------------ | ------------ | --------------- |
| 1        | 低于35岁   | 有效         | 26、12       | 月保险费 = 2.15 |
| 2        | 35-59岁    | 有效         | 37           | 月保险费 = 3.37 |
| 3        | 大于60岁   | 有效         | 65、90       | 月保险费 = 6.50 |
| 4        | 负年龄     | 无效         | -23          | 警告信息        |

### 5.3.2 因果图分析

#### 基本含义

因果图法是一种系统化黑盒测试用例设计方法，通过**分析输入条件（原因）与输出结果（效果）之间的逻辑关系来生成测试用例**。

原理：软件的输入和输出之间存在因果逻辑关系，可以用因果图刻画；因果图可从规格说明书中获得

基本流程：==生成因果图==-> ==建立判定表==-> ==生成测试用例==

$$
规格说明书 \rightarrow 因果图 \rightarrow 判定表 \rightarrow 测试用例
$$

#### 原因、结果与逻辑关系

| 元素 | 含义 |
| ---- | ---- |
| 原因 C | 输入条件或输入条件的等价类 |
| 结果 E | 输出结果或系统动作 |

原因和结果的 4 种基本关系：恒等；非；或；与。

<img src="/pintu-fulicat.com-1782111974334.png" alt="pintu-fulicat.com-1782111974334" style="zoom:33%;" />

#### 约束

课件列出 4 种输入约束和 1 种输出约束。

| 约束 | 含义 |
| ---- | ---- |
| 互斥 E | 多个原因不能同时成立，最多一个成立 |
| 包含 I | 多个原因中至少一个必须成立，不能全为 0 |
| 唯一 O | 多个原因中必须有且只有一个成立 |
| 要求R | 当 C1 成立时，C2 也必须成立。 |
| 输出约束-屏蔽M | 当E1是1时，E2必须是0；当E1是0，E2的值不定 |

![pintu-fulicat.com-1782112270467](/pintu-fulicat.com-1782112270467.png)

#### 因果图处理过程

1. 分析需求规格：确定所有输入条件(原因)和输出结果(效果)
2. 建立因果图：绘制原因与效果之间的逻辑关系图
3. 标注约束条件：识别原因之间或结果之间的约束关系
4. 转换为判定表：将因果图转换为决策表(判定表)
5. 生成测试用例：根据判定表中的每一列设计测试用例

#### 因果图主观题答题模板

1. 从题干提取所有原因 C1、C2、... 和结果 E1、E2、...。
2. 标注原因与结果之间的恒等、非、或、与关系。
3. 标注原因之间或结果之间的约束，如互斥、包含、唯一。
4. 枚举原因组合，删除违反约束或不可能的组合。
5. 将有效组合转换为判定表。
6. 每条有效规则生成一个测试用例，条件项作为输入，动作项作为预期输出。

常见扣分点：

- 不列原因/结果编号；
- 忘记删除违反约束的组合；
- 因果图和判定表不一致；
- 判定表规则没有转化为测试用例。

### 5.3.3 判定表法

#### 判定表组成

第二次小测已考：判定表包含条件项和动作桩/动作项。

| 组成 | 含义 |
| ---- | ---- |
| 条件桩 | 列出所有条件 |
| 条件项 | 每条规则中条件的取值 |
| 动作桩 | 列出所有可能动作 |
| 动作项 | 每条规则中应执行的动作 |
| 规则 | 条件取值组合及其对应动作，一列通常是一条规则 |

课件表述：原因是判定表中的条件，结果是判定表中的行动；判定规则是原因与结果的组合。

- 判定表中的条件：测试用例输入条件；
- 判定表中的行动：测试用例预期输出；
- ==一条判定规则==：一个测试用例。

<img src="/截屏2026-06-22 15.25.41.png" alt="截屏2026-06-22 15.25.41" style="zoom:33%;" />

#### 判定表简化

- 简化目标：合并相似规则；
- 相似规则判断：两条或以上规则具有相同动作，并且条件项之间高度相似，可以合并；
- `-` 表示合并后该条件项与取值无关，称为无关条件。

#### 判定表法主观题答题模板

1. 列条件桩：把所有输入条件写成行。
2. 列动作桩：把所有可能输出或处理动作写成行。
3. 枚举条件组合，形成初始规则列。
4. 删除不可能规则或违反约束的规则。
5. 根据规则填写动作项。
6. 合并动作相同且条件高度相似的规则，用 `-` 表示无关条件。
7. 每条最终规则生成一个测试用例。

### 5.3.4 边界值分析

#### 基本思想

边界值分析法是对输入域边界值进行测试的黑盒测试方法，通常==作为等价类划分的补充==，测试用例来自等价类的边界。

软件的两个主要缺陷源：1）条件；2）边界

使用方法：

- 首先确定边界情况；通常输入和输出等==价类的边界==，就是重点测试的边界情况
- 选取**正好等于、刚刚大于、刚刚小于**边界的值作为测试数据；
- 不选等价类中的任意典型值替代边界值。

边界值分析法缺点：仅适用于有明确范围的输入，不适用于无边界的情况；不覆盖所有逻辑错误，**需要结合等价类划分**等其他方法

常见边界值例子：数值范围的最小值和最大值；报表第一行、最后一行；第一列、最后一列；文件第一个记录、最后一个记录。

#### 测试用例数量公式

基本边界测试：

$$
4n+1
$$

健壮性边界测试：

$$
6n+1
$$

其中 $n$ 是存在边界值的参数个数。

边界条件测试：
$$
3m
$$
m 是边界条件数。

| **方法**       | **公式** | **覆盖范围**        | **测试力度** | 取值方法                           |
| -------------- | -------- | ------------------- | ------------ | ---------------------------------- |
| 基本边界测试   | 4n+1     | 只覆盖有效数据      | 较弱         | min；min+1；max-1；max             |
| 健壮性边界测试 | 6n+1     | 有效数据 + 无效数据 | 中等         | 再加 min-、max+                    |
| 边界条件测试   | 3m       | 有效数据 + 无效数据 | 更强         | 边界值 - 1；边界值本身；边界值 + 1 |

#### 边界条件测试补充

确定边界条件：

1. **每次只考虑一个参数的边界，固定其他参数**
2. ==补充确定的关联边界值==

#### 单变量、多变量、分段区间

| 情况 | 答题方式 |
| ---- | -------- |
| 单变量范围 | 取 min、min+、nom、max-、max，健壮性再加 min-、max+ |
| 多变量范围 | 一次让一个变量取边界，其他变量取正常值 |
| 分段区间 | 对每个分段的起点、终点及相邻值确定边界 |
| 有关联边界 | 先识别关联约束，再补充组合边界 |

#### 边界值分析主观题答题模板

1. 写出每个输入变量的有效范围。
2. 找出每个变量的下边界、上边界和必要的分段边界。
3. 选择基本边界或健壮性边界，并写明公式。
4. 对每个变量列出边界附近取值。
5. 生成测试用例表：编号、输入、选择理由、预期输出。
6. 对无效边界值写清错误提示或拒绝处理。

## 5.4 本章主观题方法总表

| 方法 | 适用场景 | 输入信息 | 输出结果 | 答题步骤 | 常见扣分点 |
| ---- | -------- | -------- | -------- | -------- | ---------- |
| 等价类划分 | 输入有范围、集合、个数、规则 | 输入条件、规格说明 | 等价类表、测试用例 | 划分有效/无效类，选代表值，写预期 | 无效类未单独覆盖 |
| 因果图 | 输入条件间有逻辑组合 | 原因、结果、约束 | 因果图、判定表、用例 | 提取 C/E，画关系，转判定表 | 忽略约束 |
| 判定表 | 多条件多动作规则 | 条件和动作 | 判定表、简化规则 | 枚举规则，删不可能，合并相似 | 条件项/动作项混淆 |
| 边界值分析 | 输入/输出有边界 | 范围、区间、分段点 | 边界测试数据 | 找边界，取边界附近值，写预期 | 只取中间值，不测边界 |

## 5.5 小测关联

第二次小测已考：

- 判定表包含条件项、动作桩；
- 年龄验证中无效等价类；
- 边界值分析 $4n+1$ 的测试数据选择；
- 黑盒与白盒的辨析。

## 5.6 本章复习检查题

### 填空题

1. 黑盒测试是一种基于________、不要求考察代码、以用户视角进行的测试。
2. 因果图法通过分析输入条件（________）与输出结果（________）之间的逻辑关系生成测试用例。
3. 健壮性边界测试公式为________。

### 选择题

1. 对输入范围 [1,100] 做基本边界测试，最典型的取值应包含：A. 50 B. 1、2、99、100 和正常值 C. 仅 1 和 100 D. 随机 5 个值
2. 判定表中一条规则通常对应：A. 一个测试用例 B. 一个模块 C. 一个缺陷 D. 一个代码分支

### 判断题

1. 每个无效等价类都应尽量单独覆盖。
2. 边界值分析通常是等价类划分的补充。
3. 因果图转换为判定表时，不需要考虑约束。

### 参考答案

1. 填空：规格说明；原因、效果/结果；$6n+1$。
2. 选择：B；A。
3. 判断：对；对；错。



## **第7章03 客观题必背清单**

## **A. 填空题重点**

1. 黑盒测试是一种基于规格说明、不要求考察代码、以用户视角进行的测试。
2. 黑盒测试又称功能测试、基于规格说明的测试。
3. 黑盒测试的实施步骤：熟悉规格说明书、生成测试用例、执行测试、判定测试结果。
4. 黑盒测试是一种基于需求的测试。
5. 黑盒测试前提：需求规格说明书经过仔细评审，隐含需求已经明确化。
6. 黑盒测试实施包括：正面测试、负面测试、边界值测试。
7. 正面测试证明系统能做它应该做的事。
8. 负面测试确保系统不会做它不应该做的事。
9. 黑盒测试用例设计方法包括：等价类划分法、因果图/判定表法、边界值分析法等。
10. 等价类是输入域的某个子集。
11. 有效等价类是满足规格说明的输入数据集合。
12. 无效等价类是不满足输入要求的输入数据集合。
13. 每个有效等价类都要覆盖。
14. 每个无效等价类都要单独覆盖。
15. 因果图中 Ci 表示原因，Ei 表示结果。
16. 因果图四种基本关系：恒等、非、或、与。
17. 因果图输入约束：互斥、包含、唯一、要求。
18. 因果图输出约束：屏蔽。
19. 判定表中的条件对应原因，行动对应结果。
20. 一条判定规则对应一个测试用例。
21. 判定表中 “—” 表示无关条件。
22. 边界值分析是对输入域边界值进行测试的黑盒测试方法。
23. 边界值分析通常作为等价类划分的补充。
24. 软件两个主要缺陷源：条件和边界。
25. 基本边界测试公式：4n+1。
26. 健壮性边界测试公式：6n+1。
27. 边界条件测试公式：3m。

------

## **B. 选择题重点**

| **问法**                       | **答案方向**                                |
| ------------------------------ | ------------------------------------------- |
| 黑盒测试依据是什么？           | 规格说明、需求、输入输出                    |
| 黑盒测试又叫什么？             | 功能测试、基于规格说明的测试                |
| 黑盒测试适用阶段有哪些？       | 单元、集成、系统、回归                      |
| 黑盒测试实施有哪些？           | 正面测试、负面测试、边界值测试              |
| 哪些属于黑盒测试用例设计方法？ | 等价类、因果图/判定表、边界值、正交、场景等 |
| 有效等价类用于检验什么？       | 程序是否满足规格说明                        |
| 无效等价类用于检验什么？       | 程序是否能处理非法输入                      |
| 因果图四种基本关系是什么？     | 恒等、非、或、与                            |
| 因果图输入约束有哪些？         | 互斥、包含、唯一、要求                      |
| 因果图输出约束是什么？         | 屏蔽                                        |
| 判定表中“—”表示什么？          | 无关条件                                    |
| 边界值分析取哪些值？           | 边界值、刚小于、刚大于                      |
| 4n+1适用于什么？               | 基本边界测试                                |
| 6n+1适用于什么？               | 健壮性边界测试                              |
| 3m适用于什么？                 | 边界条件测试                                |

------

## **C. 判断题易错点**

| **说法**                               | **正误** | **原因**                     |
| -------------------------------------- | -------- | ---------------------------- |
| 黑盒测试需要深入分析代码内部结构       | 错       | 黑盒测试不要求考察代码       |
| 黑盒测试只适用于系统测试               | 错       | 可用于单元、集成、系统、回归 |
| 黑盒测试可以发现因果关系等逻辑缺陷     | 对       | 课件明确提到                 |
| 黑盒测试前需求规格说明不需要评审       | 错       | 前提是需求规格说明已仔细评审 |
| 正面测试用于证明系统能做它应该做的事   | 对       | 合法输入、正常条件           |
| 负面测试用于确保系统不会做不该做的事   | 对       | 非法输入、异常条件           |
| 负面测试通常没有覆盖率                 | 对       | 课件比较表中说明             |
| 等价类划分的目标是穷举所有输入         | 错       | 目标是减少用例数量           |
| 无效等价类可以全部合并到一个测试用例里 | 错       | 每个无效等价类要单独覆盖     |
| 等价类划分不需要考虑输出条件           | 错       | 考虑输出条件有助于精细划分   |
| 因果图可由需求规格说明书获得           | 对       | 输入输出因果关系来自规格说明 |
| 判定表中一条判定规则对应一个测试用例   | 对       | 课件明确说明                 |
| 判定表中的“—”表示该条件取值无效        | 错       | 表示该条件与结果无关         |
| 边界值分析是白盒测试                   | 错       | 是黑盒测试                   |
| 边界值分析可作为等价类划分的补充       | 对       | 测试用例来自等价类边界       |
| 边界值分析能覆盖所有逻辑错误           | 错       | 仍需结合其他方法             |
| 4n+1包括无效边界数据                   | 错       | 基本边界测试只覆盖有效数据   |
| 6n+1包括有效和无效边界数据             | 对       | 健壮性边界测试               |

------

## **第7章03 主观题方法总表**

| **方法**   | **适用场景**                         | **输入信息**               | **输出形式**          | **答题关键**                 |
| ---------- | ------------------------------------ | -------------------------- | --------------------- | ---------------------------- |
| 等价类划分 | 输入规则、范围、格式、集合、列表     | 规格说明中的输入条件       | 等价类表 + 测试用例表 | 有效类覆盖，无效类单独覆盖   |
| 因果图     | 多个输入条件共同决定多个输出         | 原因、结果、逻辑关系、约束 | 因果图/逻辑表达式     | 找清原因、结果、约束         |
| 判定表     | 多条件组合决策                       | 条件项和动作项             | 判定表 + 测试用例     | 删除不可能组合，合并相似规则 |
| 边界值分析 | 有范围、长度、数量、分段、日期等边界 | 参数范围和边界条件         | 边界测试数据表        | 取边界前、边界点、边界后     |

------

## **第7章03 最终记忆主线**

这章可以用一句话概括：

黑盒测试是基于规格说明和用户视角的测试，重点通过正面测试、负面测试和边界值测试来检查需求是否实现；其核心用例设计方法包括等价类划分、因果图/判定表和边界值分析，其中等价类解决输入域划分问题，因果图/判定表解决多条件组合逻辑问题，边界值分析解决边界附近容易出错的问题。

本章最需要优先掌握的是：

1. **黑盒测试定义、别名、特点和步骤**；
2. **正面测试 vs 负面测试**；
3. **有效等价类 vs 无效等价类**；
4. **六类等价类划分准则**；
5. **每个有效等价类覆盖，每个无效等价类单独覆盖**；
6. **因果图四种关系：恒等、非、或、与**；
7. **因果图五类约束：互斥、包含、唯一、要求、屏蔽**；
8. **判定表条件、行动、规则的含义**；
9. **边界值分析的取值原则**；
10. **4n+1、6n+1、3m 三个公式及适用场景**。
